  <%= form_with url: send("#{controller.action_name}_url"), method: :get do |f| %>
    <% fields.each do |field| %>
      <%= render "services/search_fields/#{field[:type]}", type: field[:sub_type], f: f %>
    <% end %>

    <%= f.submit "Search", class: 'btn btn-primary mt-3' %>
  <% end %>